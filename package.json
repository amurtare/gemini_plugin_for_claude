{
  "name": "gemini-plugin-cc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Use Google Gemini CLI from Claude Code to review code or delegate tasks.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
